# WuRenji 运行时环境变量模板
# 安装脚本自动写入 /etc/wurenji/wurenji.env
# 此文件供脚本参考和手动调试使用
#
# systemd 服务文件通过安装时 sed 渲染获得实际路径
# 不依赖此文件进行变量展开

# 运行用户（Ubuntu 默认 ubuntu，RPi OS 默认 pi）
APP_USER=ubuntu
APP_GROUP=ubuntu

# 项目目录
FLY_DIR=/home/ubuntu/fly
RUNS_DIR=/home/ubuntu/fly/runs
LOGS_DIR=/home/ubuntu/fly/logs

# 各服务配置文件路径（相对于 FLY_DIR）
FLY_ACQ_CONFIG=configs/pi_ubuntu.yaml
FLY_API_CONFIG=configs/service.yaml
FLY_FSM_CONFIG=configs/fsm.yaml
