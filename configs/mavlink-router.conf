[General]
ReportStats=false

# Upstream link (serial/udp) is configured via CLI or by adding a UartEndpoint/UDPEndpoint
# section that points to the flight controller or SITL. This file focuses on the fan-out.

[UdpEndpoint gcs]
Mode=Normal
Address=127.0.0.1
Port=14550

[UdpEndpoint companion]
Mode=Normal
Address=127.0.0.1
Port=14551
